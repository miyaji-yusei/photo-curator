# Nuxt
.nuxt/
.output/
.data/
.cache/
dist/

# Dependencies
node_modules/

# Tauri / Rust
src-tauri/target/
# gen/android は旧Tauri Androidビルド一式（2026-09-17 commit 3cba50e で除去済み、
# app-android/ が正）。schemas含め生成物なのでまるごと無視する。
src-tauri/gen/

# Local files
.env
.env.*
!.env.example
*.log
.DS_Store
.idea/
.vscode/

.claude/tmp/
# UniFFI の生成物。バインディングはビルドで作るので追わない。
core/bindings/
core/target/

# 端末に入れるために組んだ成果物。**リポジトリには置かない。**
# 1 本 320MB あり、GitHub の 100MB 制限に引っかかって push できなくなる。
# 実際に一度これで詰まり、履歴を積み直す羽目になった。
build-out/
*.apk

# core-wasm の出力（wasm-pack build）。pkg/ 自体に .gitignore（*）があるが、
# core/ と揃えて明示しておく。
core-wasm/target/
core-wasm/pkg/
