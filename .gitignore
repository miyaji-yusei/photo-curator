# Nuxt
.nuxt/
.output/
.data/
.cache/
dist/

# Dependencies
node_modules/

# Tauri / Rust
src-tauri/target/
# 生成されるスキーマだけ無視する。**gen/android は追跡する**：
# Kotlin プラグインと AndroidManifest の権限をこの下に置くので、
# 無視すると clone しただけではビルドできなくなる。
src-tauri/gen/schemas/

# Local files
.env
.env.*
!.env.example
*.log
.DS_Store
.idea/
.vscode/

.claude/tmp/
# UniFFI の生成物。バインディングはビルドで作るので追わない。
core/bindings/
core/target/

# 端末に入れるために組んだ成果物。**リポジトリには置かない。**
# 1 本 320MB あり、GitHub の 100MB 制限に引っかかって push できなくなる。
# 実際に一度これで詰まり、履歴を積み直す羽目になった。
build-out/
*.apk
